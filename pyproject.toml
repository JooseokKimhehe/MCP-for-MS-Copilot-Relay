[project]
name = "mcp-server-py"
version = "0.1.0"
description = "Minimal MCP server implementation (Python)"
requires-python = ">=3.12.10,<3.13"
dependencies = [
  "fastapi>=0.95.0",
  "httpx>=0.27.0",
  "uvicorn[standard]>=0.20.0"
]

[tool.poetry]
# Optional: simple marker for editors; project metadata above is primary
